// app/schedule/components/AIScheduleConfigModal.js
"use client";

// AI Scheduling is disabled - minimal stub component
export default function AIScheduleConfigModal({
  isOpen,
  onClose,
  onGenerate,
  childSubjects = [],
  selectedChildrenIds = [],
  allChildren = [],
  isGenerating = false
}) {
  // AI Scheduling is disabled - return null to hide modal
  return null;
}
